package model.device;

public class CarDiagnostic extends Device
{
    public CarDiagnostic(int guarantee, int id, String ipAddress, String macAddress, String model)
    {
        super(guarantee, id, ipAddress, macAddress, model);
    }
}
