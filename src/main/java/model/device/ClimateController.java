package model.device;

public class ClimateController extends Device
{
    public ClimateController(int guarantee, int id, String ipAddress, String macAddress, String model)
    {
        super(guarantee, id, ipAddress, macAddress, model);
    }
}
